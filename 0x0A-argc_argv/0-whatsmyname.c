#include <stdio.h>

 /**
  * main - it does what is does and yet teh function is
  * @argc: Count arguments
  * @argv: Arguments
  *
  * Return: Always 0 (Success)
  */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
